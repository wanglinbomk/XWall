package com.boggle.wall.moudle.main.fragment.home

import androidx.lifecycle.ViewModel

class HomeViewModel : ViewModel() {
    // TODO: Implement the ViewModel
    /**
     *      RetrofitHelper.getAppAPI().getData(
    Constants.BASE_KEY, "city", "", "", "", "",
    false, "", 1, 20)
    .compose(NetworkScheduler.compose())
    .subscribe(object : RequestCallbackV2<MutableList<DataEntity>>(this) {
    override fun success(data: MutableList<DataEntity>?) {
    Toast.makeText(this@MainActivity, data?.size.toString(), Toast.LENGTH_SHORT)
    .show()
    }

    override fun failure(msg: String) {
    Log.i("xyz", msg + "fuck");
    }
    })
     */
}